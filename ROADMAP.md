# Пошаговый план разработки (Roadmap)

### Этап 1: Инициализация и Миграция базового кода
- [x] Инициализировать Git-репозиторий в `D:\sch-edu-server`.
- [x] Скопировать исходный код прототипа из `D:\py-env\hamming_web` (файлы `app.py`, `logic.py`, папки `templates` и `static`).
- [x] Подготовить виртуальное окружение (`venv`) и файл `requirements.txt`.

### Этап 2: Адаптация под компиляцию (PyInstaller)
- [x] Модифицировать `app.py`: добавить функцию динамического определения абсолютных путей до папок `templates` и `static` (использование переменной `sys._MEIPASS` при запуске из `.exe`).
- [x] Настроить SQLAlchemy так, чтобы файл базы данных всегда сохранялся в текущей рабочей директории операционной системы (CWD), чтобы данные не пропадали после перезапуска скомпилированного сервера.
- [x] Написать скрипт `build_windows.bat` для автоматической сборки проекта под Windows.

### Этап 3: Внедрение интерактивной WebAssembly песочницы (Pyodide)
- [x] Подключить легковесный редактор кода на фронтенд (CodeMirror).
- [x] Подключить библиотеку `pyodide.js` в шаблоны.
- [x] Создать миссию/урок, где ученик должен написать функцию на Python.
- [x] Написать JS-скрипт, который перехватывает написанный код, выполняет его через Pyodide со скрытыми `assert` тестами, и в случае успеха делает POST-запрос на сервер для записи результата в базу.

### Этап 4: Оптимизация Базы Данных и Учительской панели
- [x] Добавить `engine_connect` слушатель в SQLAlchemy для принудительного включения режима `PRAGMA journal_mode=WAL;` в SQLite при старте приложения.
- [x] Доработать страницу `/teacher`: добавить обновление, графики успеваемости класса и детальные логи.

### Этап 5: Создание контента и Финальное Тестирование
- [x] Разработка уроков в соответствии с [требованиями](LESSON_REQUIREMENTS.md) (включая уроки для 10 класса по информационному праву).
- [ ] Запуск `build_windows.bat`, проверка работоспособности получившегося `server.exe` на "чистой" Windows машине.
- [ ] Клонирование репозитория на машину с ALT Linux, написание скрипта `build_linux.sh` и сборка бинарника.
- [ ] Тестирование нагрузки (симуляция 30 одновременных входов учеников).
